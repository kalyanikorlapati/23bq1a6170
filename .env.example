# Campus Notifications Frontend — Environment Variables
#
# Copy this file to .env.local and fill in values for your environment.
# The frontend runs standalone (mock data) if VITE_API_BASE_URL is not set.

# URL of the Express backend (leave blank for standalone / mock mode)
# VITE_API_BASE_URL=http://localhost:3001
